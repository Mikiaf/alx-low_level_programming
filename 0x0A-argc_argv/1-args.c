#include "main.h"
#include <stdio.h>
/**
 * main- print the number arguments passed in
 * @cout: is int
 * @becout: is string
 *
 * Return:0
 */
int main(int cout, char *becout[])
{
	(void)becout;
	printf("%d\n", cout);
	return (0);
}
